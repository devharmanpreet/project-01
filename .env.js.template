/**
 * ============================================================
 * Environment Variables (.env.js)
 * ============================================================
 * 
 * DO NOT COMMIT THIS FILE TO GIT!
 * This file contains sensitive API keys.
 * 
 * Setup Instructions:
 * 1. Get your Gemini API key from https://ai.google.dev/
 * 2. Replace 'your_api_key_here' with your actual key
 * 3. Save this file
 * 4. Refresh the app
 * 5. API features will automatically activate!
 * 
 * SECURITY NOTE:
 * - Never share your API key
 * - Never commit .env.js to version control
 * - It's already in .gitignore
 * - For production, use backend environment variables
 */

const ENV = {
    // Your Google Gemini API Key
    // Get it from: https://ai.google.dev/
    // 1. Visit the link above
    // 2. Click "Get API Key"
    // 3. Create or select a project
    // 4. Copy the API key
    // 5. Paste it below (between the quotes)
    GEMINI_API_KEY: 'your_api_key_here',
    
    // Optional: API endpoint for future backend integration
    API_ENDPOINT: '',
    
    // Optional: Google Sheets ID for saving responses
    GOOGLE_SHEET_ID: '',
    
    // Enable debug logging (true/false)
    DEBUG_MODE: false
};
